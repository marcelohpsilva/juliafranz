/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  distDir: 'out',
  basePath: '/juliafranz',
  assetPrefix: '/juliafranz/',
  reactStrictMode: true,
};

module.exports = nextConfig;
